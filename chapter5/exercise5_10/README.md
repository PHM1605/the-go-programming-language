## Topological sorting
Sort the list of courses so that sooner course(s) are presiquisites of latter courses\
Rewrite `toposort` to use maps instead of slice and eliminate `sort`
```sh
go run main.go
```