## Given HTML Node, list of elements' names => give all elements that match one of those names
To illustrate `variadic function` \
Intall missing libraries
```sh
go mod init exercise5_17
go get golang.org/x/net/html
```
Run
```sh
go run main.go 
```