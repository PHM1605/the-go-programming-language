## Deferred `trace()` function that called once immediately `onStart()` and do something else when main function exit `onExit()`
NOTE: this is important to `LOG TIME ONCE` when trace logging time around a long function call 

```sh
go run main.go
```
