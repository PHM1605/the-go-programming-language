## Read HTML file, print all elements found in that HTML with hierarchy
### NEW: Turn `depth` to a shared variable of anonymous function inside `outline` function
To install missing lib package
```sh
go mod init exercise5_12
go get golang.org/x/net/html
```

Run
```sh
go run main.go http://gopl.io
```