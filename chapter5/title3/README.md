## Return text of the first `title` in `header` element; return error if not have exactly one 
Install missing libs 
```sh
go mod init title3
go get golang.org/x/net/html
```

Run
```sh
go run main.go
```