## Topological sorting
Sort the list of courses so that sooner course(s) are presiquisites of latter courses\
NEW: improve the code to REPORT internal cycle
- `linear algebra` is prerequisite of `calculus`
- `calculus` is also prerequisite of `linear algebra`

To run program
```sh
go run main.go
```