## Build a CLI for comics
NOTE: We'll download information from URL into `json` at first run `

Searching for information of a comic with term e.g. `apple` in content or title
```sh
go run . apple
```
