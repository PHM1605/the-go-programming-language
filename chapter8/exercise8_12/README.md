## Chat server
NEW: broadcaster in server stores `channels` AND `who` owns that channel too

NEW: when Client joins the chat we inform other Clients

- go to `server`, then `go run main.go`
- go to `client`, then `go run main.go` 
- go to `client`, then `go run main.go` to open 2nd Client
Start chatting