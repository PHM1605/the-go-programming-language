## Compare the speed of SERIAL and PARALLEL in `mandelbrot` program

Go to `serial` folder and run
```sh
go run main.go > serial.png
```
Go to `parallel` folder and run 
```sh
go run main.go > parallel.png
```

and check the speed
- serial: 262ms
- parallel: 60ms
