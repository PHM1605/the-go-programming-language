## Chat server
NEW: Clients now will have specified names

- go to `server`, then `go run main.go`
- go to `client`, then `go run main.go Ken` 
- go to `client`, then `go run main.go Alice` to open 2nd Client
Start chatting