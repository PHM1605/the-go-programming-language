## Web crawling in parallel
NEW: optional channel to `Cancel` in the `http.Request` struct

Install missing libraries
```sh
go mod init exercise8_10
go get golang.org/x/net/html
```

Run 
```sh
go run main.go http://gopl.io/
```
