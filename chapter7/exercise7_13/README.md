## Extend the `Expr` interface to have a `String()` method; so that when `Parse()` back from that string returns the expression
Initialization
```sh
go mod init exercise7_13
```

Run 
```sh
go mod init exercise7_13
```
